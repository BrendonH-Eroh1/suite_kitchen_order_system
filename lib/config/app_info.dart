/// Single source of truth for the user-facing app version, shown on the
/// KDS setup screen. Bump on every build pushed to GitHub.
const String kAppVersion = '0.1.0';

/// Product name shown in the UI.
const String kProductName = 'Kitchen Orders';
